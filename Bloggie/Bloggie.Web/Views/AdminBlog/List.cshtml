@model List<BlogPost>

<div class="bg-secondary bg-opacity-10 py-2">
    <div class="container">
        <h1>Show All Blog Posts - Admin Functionality</h1>
    </div>
</div>
<div class="container py-5">
    @if (Model != null && Model.Any())
    {
        <table class="table">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Heading</th>
                    <th>Page Title</th>
                    <th>Publish Date</th>
                    <th>Author</th>
                    <th>Tags</th>
                    <th> </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var blog in Model)
                {
                    <tr>
                        <td>@blog.Id</td>
                        <td>@blog.Heading</td>
                        <td>@blog.PageTitle</td>
                        <td>@blog.PublishDate</td>
                        <td>@blog.Author</td>
                        <td>
                            <div class="d-flex">
                                @foreach (var tag in blog.Tags)
                                {
                                    <span class="badge bg-secondary me-1">@tag.Name</span>
                                }
                            </div>
                        </td>
                        <td>
                            <a asp-area="" asp-controller="AdminBlog" asp-action="Edit" asp-route-id="@blog.Id">Edit</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>No blog posts found!</p>
    }
</div>